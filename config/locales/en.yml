en:
  label_hcs_cf_id: Custom Field
  label_hcs_enabled: Enabled
  activerecord:
    errors:
      models:
        issue:
          attributes:
            base:
              must_be_synced: "The field « Customer » in this form should contain at least the same contact as the field « Helpdesk Contact » (%{contact})"
